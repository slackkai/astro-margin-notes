---
/**
 * 资料库卡片：书 / 论文 / 工具 / 链接
 */
import Tags from './Tags.astro';

const TYPE_LABEL: Record<string, string> = {
  book: '📖 书',
  paper: '📄 论文',
  tool: '🔧 工具',
  course: '🎓 课程',
  article: '📰 文章',
  video: '🎬 视频',
  dataset: '🗃️ 数据集',
  other: '🔗 其他',
};
const STATUS_LABEL: Record<string, string> = { todo: '待读', reading: '在读', done: '已读' };

interface Props {
  href: string;
  title: string;
  type: string;
  url?: string;
  author?: string;
  rating?: number;
  status?: string;
  summary?: string;
  cover?: string;
  tags?: string[];
  rotate?: number;
  index?: number;
  transitionName?: string;
}
const { href, title, type, url, author, rating, status, summary, cover, tags = [], rotate = 0, index = 0, transitionName } =
  Astro.props;
---

<article class="hand-card lib-card reveal" style={`--r:${rotate}deg; --i:${index}`}>
  <svg class="clip" viewBox="0 0 24 48" aria-hidden="true">
    <path d="M8 44 V10 a4 4 0 0 1 8 0 V38 a2 2 0 0 1 -4 0 V14" />
  </svg>
  {cover && <img class="lib-cover" src={cover} alt="" loading="lazy" decoding="async" />}
  <div class="lib-body">
    <div class="lib-top">
      <span class="lib-type">{TYPE_LABEL[type] ?? TYPE_LABEL.other}</span>
      {status && <span class="lib-status">{STATUS_LABEL[status] ?? status}</span>}
    </div>
    <h3 class="lib-title" transition:name={transitionName}><a href={href}>{title}</a></h3>
    {author && <p class="meta lib-author">{author}</p>}
    {
      rating && (
        <p class="lib-rating" aria-label={`评分 ${rating} / 5`}>
          {'★'.repeat(rating)}
          <span class="dim">{'★'.repeat(5 - rating)}</span>
        </p>
      )
    }
    {summary && <p class="lib-summary">{summary}</p>}
    <div class="lib-foot">
      {tags.length > 0 && <Tags tags={tags} small />}
      {
        url && (
          <a href={url} target="_blank" rel="noopener" class="hand-btn small">
            打开 →
          </a>
        )
      }
    </div>
  </div>
</article>

<style>
  .lib-card {
    display: flex;
    flex-direction: column;
    overflow: visible;
    transform: rotate(var(--r));
    height: 100%;
  }
  .lib-card:hover {
    transform: rotate(calc(var(--r) - 0.6deg)) translate(-1px, -1px);
    box-shadow: var(--shadow-hand);
  }
  .lib-cover {
    width: 100%;
    aspect-ratio: 16/9;
    object-fit: cover;
    border-bottom: 2px solid var(--pencil);
    border-radius: 200px 12px 0 0 / 12px 200px 0 0;
  }
  .lib-body {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding: 1rem 1.2rem 1.1rem;
    flex: 1;
  }
  .lib-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
  }
  .lib-type {
    color: var(--pencil-60);
  }
  .lib-status {
    padding: 0 0.5rem;
    border: 2px solid var(--pencil);
    border-radius: var(--radius-wobble-sm);
    background: var(--postit-2);
    color: var(--pencil);
    font-family: var(--font-heading);
  }
  .lib-title {
    font-size: 1.3rem;
  }
  .lib-title a:hover {
    color: var(--marker);
  }
  .lib-author {
    margin: 0;
  }
  .lib-rating {
    margin: 0;
    color: var(--marker);
    letter-spacing: 0.1em;
    font-size: 0.95rem;
  }
  .lib-rating .dim {
    color: var(--pencil-25);
  }
  .lib-summary {
    margin: 0;
    color: var(--pencil-60);
    font-size: 1rem;
  }
  .lib-foot {
    margin-top: auto;
    padding-top: 0.6rem;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
</style>
