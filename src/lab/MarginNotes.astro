---
/**
 * 原型 8：页边批注 <Note> 与荧光笔 <Mark>。
 * 采纳后会做成 MDX 可用的组件：src/components/mdx/Note.astro、Mark.astro
 */
---

<div class="prose notes-demo">
  <p>
    缩放点积注意力把 <span class="mark">查询与键的点积除以 √d<sub>k</sub></span>
    <label class="note-wrap">
      <input type="checkbox" class="note-toggle" aria-label="展开批注" />
      <span class="note-ref">1</span>
      <span class="note">这里的 d<sub>k</sub> 是键向量的维度，不是序列长度。我第一次读时搞混了。</span>
    </label>
    ，目的是防止 softmax 进入梯度极小的区域。多头则是把投影后的子空间并行计算再拼接。
  </p>
  <p>
    位置编码用正弦函数而不是可学习向量，论文给的理由是两者效果相近，但正弦能外推到训练时没见过的长度。
    <label class="note-wrap">
      <input type="checkbox" class="note-toggle" aria-label="展开批注" />
      <span class="note-ref">2</span>
      <span class="note">
        后来的 RoPE 和 ALiBi 都在改这一处。具身场景里位置往往是连续的物理量，
        <span class="mark">这个假设值得重新审视</span>。
      </span>
    </label>
  </p>
</div>

<style>
  .notes-demo {
    position: relative;
    margin-inline: 0;
    max-width: 44rem;
  }
  /* 荧光笔：略微歪斜的半透明色块 */
  .mark {
    background: linear-gradient(104deg, transparent 0.6%, color-mix(in srgb, var(--postit) 85%, var(--marker) 15%) 1.2%, color-mix(in srgb, var(--postit) 70%, var(--marker) 30%) 98%, transparent 98.6%);
    padding: 0.05em 0.25em;
    margin: 0 -0.15em;
    border-radius: 0.2em;
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
  }
  .note-wrap {
    position: relative;
    display: inline;
  }
  .note-toggle {
    position: absolute;
    opacity: 0;
    width: 0;
    height: 0;
  }
  .note-ref {
    display: inline-block;
    font-size: 0.75em;
    font-weight: 700;
    color: var(--marker);
    vertical-align: super;
    padding: 0 0.25em;
    cursor: pointer;
    border: 1.5px solid var(--marker);
    border-radius: 50% 40% 55% 45% / 50% 55% 45% 50%;
    line-height: 1.2;
    margin-left: 0.15em;
  }
  /* 窄屏：折叠，点击展开为行内块 */
  .note {
    display: none;
    margin: 0.5rem 0;
    padding: 0.5rem 0.9rem;
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--pencil-60);
    background: var(--paper-2);
    border: 2px dashed var(--pencil-25);
    border-radius: var(--radius-wobble-sm);
  }
  .note-toggle:checked ~ .note {
    display: block;
  }
  /* 宽屏：挂在右侧页边，斜一点，带红色引线 */
  @media (min-width: 1200px) {
    .note {
      display: block;
      position: absolute;
      left: 100%;
      width: 15rem;
      margin: 0 0 0 2rem;
      padding: 0.5rem 0.75rem;
      background: var(--postit);
      border: 2px solid var(--pencil);
      border-radius: var(--radius-wobble-sm);
      box-shadow: var(--shadow-soft);
      transform: rotate(1deg);
      color: var(--pencil);
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }
    .note::before {
      content: '';
      position: absolute;
      left: -2rem;
      top: 0.9rem;
      width: 1.8rem;
      border-top: 2px dashed var(--marker);
    }
    .note-wrap:hover .note {
      transform: rotate(0deg) translate(-2px, -2px);
      box-shadow: var(--shadow-hand);
    }
  }
</style>
